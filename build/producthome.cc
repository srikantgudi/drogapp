//this file is generated by program(drogon_ctl) automatically,don't modify it!
#include "producthome.h"
#include <drogon/utils/OStringStream.h>
#include <drogon/utils/Utilities.h>
#include <string>
#include <map>
#include <vector>
#include <set>
#include <iostream>
#include <unordered_map>
#include <unordered_set>
#include <algorithm>
#include <list>
#include <deque>
#include <queue>
using namespace drogon;
std::string producthome::genText(const DrTemplateData& producthome_view_data)
{
	drogon::OStringStream producthome_tmp_stream;
	std::string layoutName{""};
	producthome_tmp_stream << "<div style=\"display:grid;grid-template-columns:1fr 2fr;gap:1rem\">\n";
	producthome_tmp_stream << "  <div>\n";
	producthome_tmp_stream << "    <h1>Categories</h1>\n";
	producthome_tmp_stream << "    <div>\n";
	producthome_tmp_stream << "      ";
{
    auto & val=producthome_view_data["categories"];
    if(val.type()==typeid(const char *)){
        producthome_tmp_stream<<*(std::any_cast<const char *>(&val));
    }else if(val.type()==typeid(std::string)||val.type()==typeid(const std::string)){
        producthome_tmp_stream<<*(std::any_cast<const std::string>(&val));
    }
}
producthome_tmp_stream<<"\n";
	producthome_tmp_stream << "    </div>\n";
	producthome_tmp_stream << "  </div>\n";
	producthome_tmp_stream << "  <div>\n";
	producthome_tmp_stream << "    <h1>Products</h1>\n";
producthome_tmp_stream<<"\n";
	producthome_tmp_stream << "    <div id=\"products\">\n";
	producthome_tmp_stream << "      -- products --\n";
	producthome_tmp_stream << "    </div>\n";
	producthome_tmp_stream << "  </div>\n";
	producthome_tmp_stream << "</div>\n";
if(layoutName.empty())
{
std::string ret{std::move(producthome_tmp_stream.str())};
return ret;
}else
{
auto templ = DrTemplateBase::newTemplate(layoutName);
if(!templ) return "";
HttpViewData data = producthome_view_data;
auto str = std::move(producthome_tmp_stream.str());
if(!str.empty() && str[str.length()-1] == '\n') str.resize(str.length()-1);
data[""] = std::move(str);
return templ->genText(data);
}
}
