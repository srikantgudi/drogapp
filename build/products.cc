//this file is generated by program(drogon_ctl) automatically,don't modify it!
#include "products.h"
#include <drogon/utils/OStringStream.h>
#include <drogon/utils/Utilities.h>
#include <string>
#include <map>
#include <vector>
#include <set>
#include <iostream>
#include <unordered_map>
#include <unordered_set>
#include <algorithm>
#include <list>
#include <deque>
#include <queue>
using namespace drogon;
std::string products::genText(const DrTemplateData& products_view_data)
{
	drogon::OStringStream products_tmp_stream;
	std::string layoutName{""};
products_tmp_stream<<"\n";
  #include <drogon/orm/Mapper.h>
  #include "Product.h"
  auto & data = products_view_data.get<drogon::orm::Result>("products");
	products_tmp_stream << "  ";
 for (auto p : data) {
	products_tmp_stream << "    <div>\n";
	products_tmp_stream << "      ";
products_tmp_stream<<p["productid"].as<int>();
	products_tmp_stream << "    </div>\n";
	products_tmp_stream << "  ";
}
if(layoutName.empty())
{
std::string ret{std::move(products_tmp_stream.str())};
return ret;
}else
{
auto templ = DrTemplateBase::newTemplate(layoutName);
if(!templ) return "";
HttpViewData data = products_view_data;
auto str = std::move(products_tmp_stream.str());
if(!str.empty() && str[str.length()-1] == '\n') str.resize(str.length()-1);
data[""] = std::move(str);
return templ->genText(data);
}
}
